type Query {
  getFilmList(input: FilmListInput): [Film]
}

input FilmListInput {
  page: Int
  resultPerPage: Int @constraint(type: "number", maximum: 10)
}

type Film {
  id: ID!
  title: String!
  description: String!
  categories: [Category] @auth(requires: USER)
}
